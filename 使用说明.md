# ProbeStudio 使用说明

> FISH 探针设计本地工具 · smFISH / smiFISH / HCR 3.0 / SNAIL FISH · 序列不出机

---

## 一、启动

在 Finder 中双击：

```
~/Documents/ChatGPT/Analysis/probe_design_gui/launch.command
```

浏览器会自动打开界面。停止服务：在启动它的终端窗口按 `Control-C`（或直接关闭该终端窗口）。

也可以用终端启动：

```bash
/opt/anaconda3/envs/Probe/bin/python -m streamlit run ~/Documents/ChatGPT/Analysis/probe_design_gui/app.py
```

---

## 二、需要哪些数据

设计 FISH 探针只需要两类 FASTA 格式的基因组序列：

| 数据 | 作用 | 什么时候需要 |
|------|------|--------------|
| **靶序列** | 探针要结合的基因/转录本（几百到几千 nt 最合适） | 每次设计时粘贴 |
| **背景基因组** | 用于过滤"会错结合到其他物种"的探针 | 一次性注册，之后反复使用 |

### ✅ 已经帮你下载好的三个基因组（可直接使用）

以下三个基因组已下载到 `genomes/` 目录，**bowtie2 索引已构建并注册完毕**，
打开界面即可在"④ 宿主/背景基因组"中直接勾选：

| 基因组 | 菌株 | 参考序列 | 文件 |
|--------|------|----------|------|
| **MTB** | *M. tuberculosis* H37Rv | NC_000962.3 | `genomes/mtb_h37rv.fna` |
| **BCG** | *M. bovis* BCG Pasteur 1173P2 | NC_008769.1 | `genomes/bcg_pasteur.fna` |
| **MSM** | *M. smegmatis* mc² 155 | NC_008596.1 | `genomes/msm_mc2155.fna` |

三个菌在探针设计中的典型用法：

- **做 MTB 探针**（临床/样本中检测结核菌）：靶标 = MTB 基因；背景勾选
  `bcg_pasteur` + `msm_mc2155` → 探针不会错结合疫苗株和污染源 smegmatis。
- **做 BCG 探针**（区分疫苗株与野毒株）：靶标 = BCG 特异区（如 RD1 区的
  Rv3871–Rv3879 等缺失区基因）；背景勾选 `mtb_h37rv` + `msm_mc2155`。
- **做 smegmatis 探针**（实验室模式菌成像）：靶标 = smegmatis 基因；背景勾选
  两个结核菌群基因组。

> 注意：上面三个是参考菌株。如果你的 BCG 是实验室使用的其他亚株
> （Tokyo / Denmark / Moreau / Glaxo，彼此有数百处 SNP 和若干区段差异），
> 告诉我亚株名，可以再下载对应基因组；对探针设计的影响通常不大，
> 但强严谨实验建议用自家菌株的序列。

### 还可能需要下载的

| 数据 | 用途 | 下载来源 |
|------|------|----------|
| 人（GRCh38）/ 小鼠（mm39）基因组 | 感染模型中过滤宿主细胞交叉反应 | NCBI：`https://www.ncbi.nlm.nih.gov/datasets/genome/` 搜组装号 → Download → "Genome sequences (FASTA)" |
| 其他分枝杆菌（如 *M. avium*、*M. bovis* 野毒株） | 提高属内特异性 | 同上 |
| 靶基因序列 | 粘贴进界面做设计 | 从上述基因组 FASTA 中截取，或用 NCBI Gene 页面下载 |

**下载姿势（NCBI）**：打开 `https://www.ncbi.nlm.nih.gov/datasets/genome/` →
搜索物种名 → 选中组装 → **Download** → 勾选 *Genome sequences (FASTA)* →
解压得到 `*_genomic.fna`。然后把文件拖进界面上的
"基因组与索引管理"上传框 → 填 ID 和名称 → 点"构建索引并注册"。

---

## 三、设计流程（五步）

1. **粘贴靶序列**：左侧"① 靶序列"选"粘贴 FASTA"，格式：
   ```
   >my_gene
   ATGACCATGATTACGCCAAG...(几百到几千 nt)
   ```
   - 方向：选 `+`（mRNA 序列本身）；如果你拿到的是基因组负链基因，可先取反向互补或直接选 `-`。
   - **建议用单个基因或一段转录本**，不要直接贴全基因组（候选数会爆炸，
     "比对数"过滤也会失去意义）。

2. **选设计方案**：smFISH（最常用）/ smiFISH / HCR 3.0 / SNAIL FISH，
   每个方案下方有一句话说明产物形态。

3. **调参数**：默认值即常用值。关键三个旋钮——
   - Tm 窗口（默认 50–70 °C）：越窄均一性越好、候选越少；
   - GC 窗口（默认 0.20–0.80）；
   - 目标探针数：候选太多时按均匀分布降采样（例如设 20–30）。

4. **勾选背景基因组**：在"④ 宿主/背景基因组"勾选要比对的基因组
   （已预置 MTB / BCG / MSM，以及你自己注册的）。

5. **点"开始设计"**，看结果：

| 结果区 | 内容 |
|--------|------|
| 指标卡 | 候选数 / 通过数 / 靶标覆盖率 / Tm 均一性 |
| 漏斗图 | 候选 → 热力学 → 特异性 → 最终，每步淘汰多少 |
| 结果表 | 每条探针的坐标、序列、Tm、GC、比对数（列头有 ⓘ 解释） |
| 覆盖图 | 探针在靶标上的分布与 Tm 着色，灰点为被淘汰候选 |
| 单条详情 | 方案专属序列（smiFISH 完整序列 / HCR P1+P2 / SNAIL primer+padlock） |
| 导出 | 全部/最终 CSV、IDT 兼容订购表、参数 JSON |

---

## 四、四种方案速查

| 方案 | 订购产物 | 注意 |
|------|----------|------|
| **smFISH** | 每条探针一个寡核苷酸（5′/3′ 标荧光） | 订购表中 `sequence` 直接可用 |
| **smiFISH** | 一条通用 FLAP 二级探针 + 所有探针带延伸段 | 先确定 readout 序列再设计；输出含完整序列 |
| **HCR 3.0** | 每个位点两条半探针 P1/P2（选通道 B1–B5） | 多色实验给不同靶标选不同通道 |
| **SNAIL FISH** | primer + padlock 两条；padlock 订购需加 5′ 磷酸化 `/5Phos/` | UGI 条码可留空（N 占位）后续填入 |

---

## 五、常见问题

**Q：启动报 `No module named ...`**
环境没有装全。修复：`PYTHONNOUSERSITE=1 /opt/anaconda3/envs/Probe/bin/pip install -e ~/Documents/ChatGPT/Analysis/probe_design_gui`（在项目目录运行）。

**Q：最终探针数量很少？**
正常。1000 nt 靶标典型产出 10–40 条。想更多：放宽 Tm 窗口、加大长度范围
（如 18–26）、或把"目标探针数"清零。

**Q：全部候选都被 Tm 淘汰？**
Tm 窗口对长度敏感。检查长度范围与 Tm 窗口是否匹配（18–24 nt 配 50–70 °C
通常没问题）；或换杂交条件（"杂交条件"里调 Na⁺/甲酰胺）。

**Q：HCR3 没有结果？**
看漏斗图哪一步归零。tile 的整体 Tm 不作为默认过滤（协议用 dTm + Gibbs），
若你手动开启了 tile Tm 窗口请关闭再试。

**Q：背景基因组能勾几个？**
任意多个，每个多一次比对步骤。三个分枝杆菌基因组（4–7 Mb）全程几秒到
几十秒；人的基因组（3 Gb）也能跑但首次注册建索引需要几分钟。

**Q：能设计真核宿主细胞里的菌（感染模型）吗？**
可以，这正是宿主过滤的用途：背景同时勾选细菌近缘基因组 + 宿主基因组
（人/鼠），双阈值过滤。

---

## 六、数据与文件位置

| 路径 | 内容 |
|------|------|
| `genomes/` | 已下载的基因组 FASTA（MTB/BCG/MSM + 你上传的） |
| `indices/` | bowtie2 索引 |
| `genome_registry.json` | 已注册的基因组清单（界面管理区读写） |
| `data/smoke/` | 自带冒烟测试数据（含索引，可在界面里体验） |
| `extracted/` | 原始参考项目存档（可删除，不影响运行） |

序列均只在本地处理，不上传任何服务器。
